CREATE TABLE `NOAA_STATION_XREF` (
    `STRUCTURE_NUMBER_008` TEXT,
    `BRIDGE_LONGITUDE` REAL,
    `BRIDGE_LATITUDE` REAL,
    `NEAREST_NOAA_STATION` REAL,
    `STATION_LONGITUDE` REAL,
    `STATION_LATITUDE` REAL,
    `DISTANCE_APART_KM` REAL
);

