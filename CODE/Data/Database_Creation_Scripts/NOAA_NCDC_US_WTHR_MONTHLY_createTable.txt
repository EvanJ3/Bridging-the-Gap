CREATE TABLE `NOAA_NCDC_US_WTHR_MONTHLY` (
  `STATION` TEXT,
  `NAME` TEXT,
  `LATITUDE` REAL,
  `LONGITUDE` REAL,
  `ELEVATION` REAL,
  `DATE` TIMESTAMP,
  `CLDD` INTEGER,
  `HTDD` INTEGER,
  `EMXP` REAL,
  `EMSD` REAL,
  `EMSN` REAL,
  `DSND` INTEGER,
  `DSNW` INTEGER,
  `PRCP` REAL,
  `SNOW` REAL,
  `EMXT` INTEGER,
  `EMNT` INTEGER,
  `TMAX` INTEGER,
  `TMIN` INTEGER,
  `AWND` REAL
)